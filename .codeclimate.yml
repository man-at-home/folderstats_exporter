---
engines:
  # golang
  gofmt:
    enabled: true
  golint:
    enabled: true
    config:
      min_confidence: 0.9
  govet:
    enabled: true
  # misc
  fixme:
    enabled: true
ratings:
  paths:
    - "**.go"
